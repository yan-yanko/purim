{
  "name": "purim-costume-generator",
  "version": "1.0.0",
  "description": "תחרות התחפושות של נחמיה - מחולל תחפושות AI",
  "scripts": {
    "start": "netlify dev",
    "build": "npm run copy-files",
    "copy-files": "mkdir -p public && cp -r src/styles public/ && cp -r src/js public/ && cp -r src/images public/"
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.4.5",
    "openai": "^4.28.4",
    "multer": "^1.4.5-lts.1",
    "cors": "^2.8.5",
    "@netlify/functions": "^2.6.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
} 